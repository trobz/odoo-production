No additional configuration is required for this module. It automatically integrates with:

* Purchase orders that use package-based pricing policies
* Supplier price lists that reference base prices
* Product supplier info records that use base prices

The module automatically overrides the default price field to use "base_price" for all relevant models.
