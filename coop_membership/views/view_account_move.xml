<?xml version="1.0" ?>
<!--
    Copyright (C) 2016-Today: La Louve (<http://www.lalouve.net/>)
    @author: Sylvain LE GAL (https://twitter.com/legalsylvain)
    License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl.html).
-->
<odoo>
    <record model="ir.ui.view" id="view_move_form">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="capital_subscription.view_move_form" />
        <field name="arch" type="xml">
            <field name="is_capital_fundraising" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
        </field>
    </record>

    <!-- Create new action to refund Capital Subscription -->
    <record
        id="action_account_invoice_refund_capital_subscription"
        model="ir.actions.act_window"
    >
        <field name="name">Refund Capital Subscription</field>
        <field name="res_model">account.move.reversal</field>
        <field name="view_mode">form</field>
        <field
            name="view_id"
            ref="capital_subscription.view_account_move_reversal_capital_subscription_form"
        />
        <field name="target">new</field>
        <field name="context">{'active_model': 'account.move'}</field>
    </record>

    <record model="ir.ui.view" id="view_account_invoice_form_inherit">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <!-- Hide classical refund button for capital subscription-->
            <xpath expr="//button[@name='action_reverse']" position="attributes">
                <attribute
                    name="invisible"
                >move_type == 'out_refund' or state != 'posted' or is_capital_fundraising
                </attribute>
            </xpath>
            <xpath expr="//button[@name='action_reverse']" position="after">
                <button
                    name="%(action_account_invoice_refund_capital_subscription)d"
                    type='action'
                    string='Refund Capital Subscription'
                    groups="coop_membership.group_refund_capital_subscription"
                    invisible="move_type == 'out_refund' or state != 'posted' or not is_capital_fundraising"
                />
            </xpath>
        </field>
    </record>
</odoo>
