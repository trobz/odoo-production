# Usage

1. Go to `Inventory > Operations > Recurrent Inventories `.
2. Click the `Execute` to create inventory adjustments for selected category groups.
3. Select one or more inventory adjustments and use the `Print Recurring Inventories` action to generate inventory reports.
