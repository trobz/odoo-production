This module gathers generic Sale Order view customizations shared by all
foodcoop instances.

Currently it hides the "Packaging Quantity" and "Packaging" columns in the
sale order line list view by default (they remain available through the
optional column selector).
