<?xml version="1.0" encoding="UTF-8" ?>
<templates id="template" xml:space="preserve">
    <t t-inherit="point_of_sale.ProductScreen" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('subpads')]" position="before">
            <t t-if="showScrapButtons and !ui.isSmall">
                <div class="scrap-buttons-row d-flex gap-2 w-100 pb-2">
                    <button
                        class="scrap-control-btn scrap-control-btn-main"
                        t-on-click="clickScrap"
                    >
                        <i class="fa fa-trash-o" /> Make Scrap Order
                    </button>
                    <button
                        class="scrap-control-btn scrap-control-btn-secondary"
                        t-on-click="clickScrapList"
                    >
                        List
                    </button>
                </div>
            </t>
        </xpath>
    </t>
</templates>
